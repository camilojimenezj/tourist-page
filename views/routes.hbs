{{> header}}
<div class="container">
  {{#each list}}
  <div class="card">
    <div class="face front">
      <img src="{{this.img}}" alt="{{this.title}}">
      <h3>{{this.title}}</h3>
    </div>
    <div class="face back">
      <h3>{{this.title}}</h3>
      {{#each this.description}}
      <p>{{this}}</p><br />
      {{/each}}
      <div class="link">
        <a class="hero__cta" href="#" data-place="{{this.title}}">COMENTARIOS.</a>
      </div>
    </div>
  </div>
  {{/each}}
  {{#if name}}
  <div class="add-card open-card">
    <img src="/assets/add-img.svg" alt="add-img">
    <h3>Añadir Lugar</h3>
  </div>
  {{/if}}
</div>
{{> modal}}
{{> modal2}}
<script src="/socket.io/socket.io.js"></script>
<script src="/js/main.js" type="module"></script>
{{> footer}}